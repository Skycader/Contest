/**
 * Among the given text,
 * find all the strings that could be good passwords,
 * that must have capital and lower case, one digit and one of !@#$%^&*
 * Find all of them
 */

const f = (text) => {
  const r =
    //;
  return;
};

module.exports = f;
